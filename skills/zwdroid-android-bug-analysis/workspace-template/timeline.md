# timeline — <bug-id>

<!--
语义时间线。与尖叫扫描通道并列,永不合并(红线 R10)。
每条格式:[精确时间戳] [userId/进程:pid] [事件] [证据 原始文件:行号]
- pid 一律附进程名与 userId;跨 pid 接续查 manifest 进程-pid 对照表。
- 时间戳避让 manifest 时钟跳变点与静默区间,两侧不可直接比较。
- 时序空洞可作证据,但须先排除损耗/静默区间(红线 R13)。
- 总量控制在一屏(约 20-30 条);超量说明窗口/tag 未收敛,退回 S2 收紧。
-->

## 事件序列
| 精确时间戳 | userId/进程:pid | 事件 | 证据 [原始文件:行号] |
|-----------|-----------------|------|---------------------|
|           |                 |      |                     |

## 制品要点(S3 制品解析,若取证范围含 trace/tombstone/dropbox)
<!-- 与时间线并列,不混入上表。每条带制品 文件:行号。 -->
-

## 时序空洞 / 异常(已排除损耗与静默区间后)
-
